#include <stdio.h>
int main ()
{
    char a;
    scanf("%d", &a);
    if (a<='a' && z>='z')
    printf("Uppercase");
    else if (a<='A' && z>='Z')
    printf("Lowercase");
    else
    printf("Not an alphabet";
    return 0;)
}