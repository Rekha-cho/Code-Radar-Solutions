#include <stdio.h>
int main ()
{
    char a,b;
    scanf("%c %c", &a,&b);
    if (a == a,e,i,o,u)
    printf("Vowel");
    if (a != a,e,i,o,u)
    printf("Consonant");
    else
    printf("Digit");
    return 0;
}